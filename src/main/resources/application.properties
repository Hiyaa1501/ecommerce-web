spring.application.name=ecommerce

# Database: Uses Render environment variables if they exist
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/emply?useSSL=false}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:hiya15}

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true